name: Run Stock Report Script

on:
  schedule:
    - cron: "8 15 * * *"   # Runs at 15:08 UTC = 20:38 IST
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies for test1.py
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt  # For test1.py

      - name: Run test1.py
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
        run: python test1.py

      - name: Install additional dependencies for Swing_Str2.py
        run: |
          pip install python-dotenv==1.0.1 scipy==1.14.1  # Additional dependencies for Swing_Str2.py

      - name: Run Swing_Str2.py
        env:
          GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
          SMTP_USERNAME: ${{ secrets.SMTP_USERNAME }}
          SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
          
        run: python Swing_Str2/Swing_Str2.py  # Updated path to include subdirectory